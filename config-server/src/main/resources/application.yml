spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mohitkaul17/config-repo.git
    bus:
      enabled: true
  rabbitmq:
    port: 5672
    host: localhost
    password: guest
    username: guest

server:
  port: 8888

logging:
  level:
    Root: INFO
    org.springframework: INFO
